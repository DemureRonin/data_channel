# Data Channel

Высокопроизводительный канал сжатых данных для межпроцессного взаимодействия с использованием разделяемой памяти.

## Сборка

```bash
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release \
         -DCMAKE_C_COMPILER=clang \
         -DCMAKE_CXX_COMPILER=clang++ \
         -DCMAKE_CXX_STANDARD=17 \
         -DCMAKE_EXE_LINKER_FLAGS="-fuse-ld=lld"
make -j4
```
## Запуск

./producer <input_file> <output_file> [--no-compress]

./consumer <output_file> [--no-compress]

#### Рекомендуется запускать одновременно, так как Consumer ожидает появления семафоров и разделяемой памяти:

#### Со сжатием
./consumer <output_file> & ./producer <input_file> <output_file>

#### Без сжатия
./consumer <output_file> --no-compress & ./producer <input_file> <output_file> --no-compress

### Выбор алгоритма сжатия
Библиотека ZFP была выбрана потому, что она гарантирует детерминированный размер выходных данных. В режиме фиксированной скорости (7 бит на значение) 280 чисел с плавающей точкой (1120 байт) сжимаются до ~245 байт, что идеально вписывается в 248-байтную полезную нагрузку разделяемой памяти. Потери данных допустимы и контролируемы.

### Методы синхронизации
Используются три именованных POSIX-семафора:

sem_empty (начальное значение 1) – управляет доступом на запись

sem_full (начальное значение 0) – сигнализирует о наличии данных

sem_done – уведомляет Producer о завершении работы Consumer

Разделяемая память представляет собой фиксированный сегмент размером 256 байт. Producer записывает данные через memcpy, Consumer читает их.

Producer ожидает семафор empty, записывает данные, затем сигнализирует семафор full. Consumer ожидает семафор full, читает данные, затем сигнализирует семафор empty.

### Компромиссы
Сжатие с фиксированной скоростью приводит к потере ~8 байт в сегменте разделяемой памяти (полезная нагрузка 248 байт, служебные данные 8 байт). Сжатие с потерями обеспечивает 4-кратное сжатие с ошибкой около 7%.

### Оптимизации
Трёхпоточный конвейер (чтение файла → сжатие → запись в разделяемую память) позволяет выполнять операции ввода-вывода, сжатия и обмена данными параллельно. Значение битрейта (7 бит на значение) было подобрано вручную, чтобы гарантировать размер сжатых данных менее 248 байт.

## Очистка

Приложение корректно обрабатывает ошибки в аргументах командной строки и прерывания (SIGINT). В случае сбоя Producer, Consumer ожидает данные в течение 5 секунд, после чего завершается по таймауту.

Тем не менее, при возникновении ошибок или повреждённых данных рекомендуется **вручную проверить, что все семафоры удалены, а процессы завершены**.
### TODO
- Сделать графический интерфейс с SDL 
- Использовать пул потоков для устранения дублирования кода и разделения процесса декомпрессии на большее количество потоков 
